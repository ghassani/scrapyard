pandaConfig = {
    system: {
        webGL: true,
        width: 1024,
        height: 768,
        scaleToFit: true,
        startScene: 'Main'
    },
    debug: {
        enabled: true,
        color: 'red'
    },
    storage: {
        id: 'com.splicedmedia.rpgtest'
    },
    analytics: {
        id: 'UA-000000-01'
    }
    //sitelock: 'pandajs.net'
};

pandaConfig.iOS = {
    // Config for iOS devices
};

pandaConfig.android = {
    // Config for Android devices
};

pandaConfig.wp = {
    // Config for Windows Phone devices
};